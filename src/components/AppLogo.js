import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  display: block;
  font-size: 50px;
`;

const AppLogo = () => (
  <Wrapper>spockoko</Wrapper>
);

export default AppLogo;
